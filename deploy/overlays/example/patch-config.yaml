apiVersion: apps/v1
kind: Deployment
metadata:
  name: hydra-operator
spec:
  template:
    spec:
      containers:
        - name: hydra-operator
          env:
            - name: HYDRA_URL
              value: "https://hydra.example.com"
            - name: HYDRA_CLUSTER_ID
              value: "example-cluster"
